<%= form_for(award) do |f| %>
  <% if award.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(award.errors.count, "error") %> prohibited this award from being saved:</h2>
      <ul>
      <% award.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <div class="field">
    <%= f.label "Award type: " %>
    <%= f.text_field :award_type %>
  </div>

   <div class="field">
    <%= f.label "Award granted to employee: " %>
    <%= f.text_field :employee_name %>
  </div>

   <div class="field">
    <%= f.label "Employee email: " %>
    <%= f.text_field :employee_email %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
